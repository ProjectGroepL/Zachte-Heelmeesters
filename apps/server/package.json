{
  "name": "@zhm/server",
  "version": "1.0.0",
  "description": "ASP.NET server api for Zachte Heelmeesters",
  "private": true,
  "scripts": {
    "prepare": "dotnet restore server.sln",
    "dev": "dotnet run --launch-profile https --project server/ZhmApi.csproj",
    "build": "dotnet publish server/ZhmApi.csproj -c Release --no-restore -o dist"
  }
}